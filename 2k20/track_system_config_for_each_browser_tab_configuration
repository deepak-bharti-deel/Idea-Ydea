
an extension which tracks the system configuration which was on different tabs,
for example, 
- we sometimes reduce/dicrease the screen brightness on the basis of the content on the screen
- different volumes on tabs with sound/stuff on the page
- so on ...
